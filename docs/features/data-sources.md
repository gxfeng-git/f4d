# 数据源管理

支持以下操作：

- 新建空数据源
- 切换当前激活数据源
- 重命名数据源
- 删除数据源
- 导出单个数据源 JSON
- 从文件导入
- 从 HTTPS 链接导入
- 作为新数据源导入
- 覆盖任意已有数据源

覆盖模式下不会影响其他数据源。

## 当前交互规则

- 导入“作为新数据源”时，必须输入新数据源名称才能执行导入。
- 导入“覆盖已有数据源”时，必须先选择目标数据源并完成二次确认。
- 删除与覆盖都只作用于目标 `sourceId`，不会影响其他数据源的数据和状态。
